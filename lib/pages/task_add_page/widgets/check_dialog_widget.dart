import 'package:flutter/material.dart';

class CheckDialogWidget extends StatelessWidget {
  const CheckDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
